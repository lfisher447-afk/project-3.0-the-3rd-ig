export const workerSource = `
const E=new TextEncoder(),D=new TextDecoder();let canceled=false,waiting=null;
const post=(type,p={})=>postMessage(Object.assign({type},p));const fail=m=>{throw Error(m)};
const b64=s=>Uint8Array.from(atob(s||''),c=>c.charCodeAt(0));const i64=(v,o)=>v.getInt32(o,false)*4294967296+v.getUint32(o+4,false);const putI64=(v,o,n)=>{v.setInt32(o,Math.floor(n/4294967296),false);v.setUint32(o+4,n>>>0,false)};
const mime=c=>({flac:'audio/flac',mp3:'audio/mpeg',m4a:'audio/mp4',mp4:'audio/mp4',ogg:'audio/ogg',opus:'audio/ogg; codecs=opus',wav:'audio/wav',webm:'audio/webm',aac:'audio/aac'})[String(c||'').toLowerCase().replace(/^\\./,'')]||'audio/*';
const nonce=(base,index)=>{const out=new Uint8Array(base);let n=index;for(let i=0;i<8;i++){const p=out.length-1-i;out[p]^=Math.floor(n%256);n=Math.floor(n/256)}return out};
const aad=(head,index)=>{const out=new Uint8Array(head.length+8);out.set(head);putI64(new DataView(out.buffer),head.length,index);return out};
async function derive(pass,salt,it){post('pulse',{stage:'DERIVING KEY'});const base=await crypto.subtle.importKey('raw',E.encode(pass),'PBKDF2',false,['deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',hash:'SHA-256',salt,iterations:it},base,{name:'AES-GCM',length:256},false,['decrypt'])}
class Reader{constructor(file,bytes){this.file=file;this.bytes=bytes;this.next=0;this.buf=new Uint8Array(0);this.at=0;this.used=0;this.refills=0}get available(){return this.buf.length-this.at}async ensure(size){while(this.available<size){if(this.next>=this.file.size)return false;const carry=this.buf.subarray(this.at),len=Math.min(this.bytes,this.file.size-this.next),fresh=new Uint8Array(await this.file.slice(this.next,this.next+len).arrayBuffer()),joined=new Uint8Array(carry.length+fresh.length);joined.set(carry);joined.set(fresh,carry.length);this.buf=joined;this.at=0;this.next+=len;this.refills++}return true}take(size){if(this.available<size)fail('Archive ended before its declared frame boundary.');const out=this.buf.subarray(this.at,this.at+size);this.at+=size;this.used+=size;return out}}
async function readHeader(reader){if(!(await reader.ensure(11)))fail('Archive is too short.');const pre=reader.take(11);if(!D.decode(pre.subarray(0,7)).startsWith('NOVAAC'))fail('This is not a compatible NovaAc archive.');const len=new DataView(pre.buffer,pre.byteOffset,pre.byteLength).getInt32(7,false);if(len<2||len>65536)fail('NovaAc header length is invalid.');if(!(await reader.ensure(len+1)))fail('Archive header is truncated.');const chunk=reader.take(len+1),head=chunk.subarray(0,len),meta={};D.decode(head).split('\\n').forEach(line=>{const p=line.indexOf('=');if(p>0)meta[line.slice(0,p).trim()]=line.slice(p+1).trim()});const nonceLen=chunk[len];if(nonceLen<8||nonceLen>32)fail('NovaAc nonce is invalid.');if(!(await reader.ensure(nonceLen)))fail('Archive nonce is truncated.');return{meta,head,nonce:reader.take(nonceLen),version:Number(meta.formatVersion||5)}}
class Parser{constructor(profile){this.b=new Uint8Array(0);this.collection='Imported NovaAc archive';this.cur=null;this.count=0;this.batch=[];this.batchBytes=0;this.batchId=0;this.limit=profile.tracks;this.byteLimit=profile.batchBytes}join(x){const n=new Uint8Array(this.b.length+x.length);n.set(this.b);n.set(x,this.b.length);this.b=n}utf(o){if(o+2>this.b.length)return null;const n=new DataView(this.b.buffer,this.b.byteOffset,this.b.byteLength).getUint16(o,false);if(o+2+n>this.b.length)return null;return[D.decode(this.b.subarray(o+2,o+2+n)),o+2+n]}var(o){let n=0,m=1;for(let i=0;i<10;i++){if(o+i>=this.b.length)return null;const q=this.b[o+i];n+=(q&127)*m;if(!Number.isSafeInteger(n))fail('Archive field exceeds browser-safe range.');if(!(q&128))return[n,o+i+1];m*=128}fail('Malformed NovaAc variable length')}need(){if(!this.cur)fail('Track field appeared before track start.');return this.cur}async flush(){if(!this.batch.length)return;const tracks=this.batch;this.batch=[];this.batchBytes=0;const batchId=++this.batchId;post('batch',{batchId,tracks});await new Promise((resolve,reject)=>waiting={batchId,resolve,reject})}async finish(){const c=this.need(),blob=c.parts.length?new Blob(c.parts,{type:mime(c.container)}):null,t={id:c.id||crypto.randomUUID(),title:c.title||'Untitled track',artist:c.artist||'Unknown artist',album:c.album||this.collection,duration:c.duration||0,addedAt:Date.now(),fileName:c.fileName||'',container:c.container||'',size:blob?.size||0,artwork:c.artwork||'',source:'novaac',downloadState:blob?'ready':'metadata',blob};this.count++;this.batch.push(t);this.batchBytes+=t.size;if(this.batch.length>=this.limit||this.batchBytes>=this.byteLimit)await this.flush();this.cur=null}async push(x){this.join(x);while(!canceled){if(this.cur?.active){if(!this.b.length)break;const n=Math.min(this.cur.remaining,this.b.length);this.cur.parts.push(this.b.subarray(0,n));this.cur.remaining-=n;this.b=this.b.subarray(n);if(this.cur.remaining)break;this.cur.active=false;continue}if(!this.b.length)break;const op=this.b[0],str=fn=>{const z=this.utf(1);if(!z)return false;fn(z[0]);this.b=this.b.subarray(z[1]);return true};if(op===1){if(!str(x=>this.collection=x))break}else if(op===2||op===3||op===24||op===35){if(!str(()=>{}))break}else if(op===16){this.cur={id:'',title:'',artist:'',album:'',duration:0,container:'',fileName:'',artwork:'',parts:[],remaining:0,active:false};this.b=this.b.subarray(1)}else if(op===17){if(!str(x=>this.need().id=x))break}else if(op===18){if(!str(x=>this.need().title=x))break}else if(op===19){if(!str(x=>this.need().artist=x))break}else if(op===20){if(!str(x=>this.need().album=x))break}else if(op===21){if(!str(x=>this.need().artwork=x))break}else if(op===22){const z=this.var(1);if(!z)break;this.need().duration=z[0]/1000;this.b=this.b.subarray(z[1])}else if(op===23){if(this.b.length<2)break;this.b=this.b.subarray(2)}else if(op===33){if(!str(x=>this.need().container=x))break}else if(op===34){if(!str(x=>this.need().fileName=x))break}else if(op===32){if(this.b.length<10)break;const z=this.var(9);if(!z)break;this.need().remaining=z[0];this.need().active=z[0]>0;this.b=this.b.subarray(z[1])}else if(op===31){this.b=this.b.subarray(1);await this.finish()}else if(op===255){this.b=this.b.subarray(1);return}else fail('Unsupported NovaAc operation '+op)}}}
async function inspect(file,profile){const reader=new Reader(file,profile.window);const h=await readHeader(reader);post('inspect',{meta:h.meta,bytes:file.size,window:profile.window});}
async function run({file,pass,profileName}){const profiles={safe:{window:1024*1024,tracks:1,batchBytes:4*1024*1024},balanced:{window:4*1024*1024,tracks:3,batchBytes:12*1024*1024},turbo:{window:16*1024*1024,tracks:8,batchBytes:32*1024*1024}},profile=profiles[profileName]||profiles.balanced;if(!pass)fail('Enter the archive Web Passphrase.');const reader=new Reader(file,profile.window),h=await readHeader(reader),proto=h.meta.protocol||'';if(h.meta.securityMode!=='WEB_PASSPHRASE')fail('This device-secure archive is tied to Android Keystore. Export a Web Passphrase archive for browser playback.');if(proto!=='NovaBytecode-v7-FramedGcm'&&proto!=='NovaBytecode-v8-FramedGcm')fail('This player supports framed v7/v8 Web Passphrase NovaAc archives.');if(!self.DecompressionStream)fail('This browser lacks streaming gzip support.');const key=await derive(pass,b64(h.meta.kdfSaltBase64),Math.max(1,Number(h.meta.kdfIterations||210000)));post('header',{meta:h.meta,total:file.size,profile:profileName,window:profile.window});const gzip=new DecompressionStream('gzip'),writer=gzip.writable.getWriter(),parser=new Parser(profile),consumer=(async()=>{const r=gzip.readable.getReader();for(;;){const q=await r.read();if(q.done)break;await parser.push(q.value)}})();let frame=0,last=performance.now(),started=last;try{for(;;){if(canceled)fail('Archive import was cancelled.');if(!(await reader.ensure(16)))fail('Archive ended before its terminator frame.');const fh=reader.take(16),view=new DataView(fh.buffer,fh.byteOffset,fh.byteLength),idx=i64(view,0),plainLength=view.getInt32(8,false),cipherLength=view.getInt32(12,false);if(idx===-1){if(plainLength||cipherLength)fail('Malformed NovaAc terminator frame.');break}if(idx!==frame||plainLength<1||plainLength>65536||cipherLength<plainLength+16||cipherLength>plainLength+32)fail('Invalid or interrupted NovaAc frame '+frame+'.');if(!(await reader.ensure(cipherLength)))fail('Archive ciphertext is truncated.');const encrypted=reader.take(cipherLength),iv=nonce(h.nonce,frame),base={name:'AES-GCM',iv},withAad={name:'AES-GCM',iv,additionalData:aad(h.head,frame)};post('pulse',{stage:'DECRYPTING',frame,bytes:reader.used,total:file.size});let plain;try{plain=await crypto.subtle.decrypt(h.version>=8?withAad:base,key,encrypted)}catch(error){if(h.version>=8&&h.meta.frameAad!=='header-index')plain=await crypto.subtle.decrypt(base,key,encrypted);else throw error}if(plain.byteLength!==plainLength)fail('Frame plaintext length mismatch.');post('pulse',{stage:'INFLATING',frame,bytes:reader.used,total:file.size});await writer.write(new Uint8Array(plain));frame++;const now=performance.now();if(now-last>120){const elapsed=(now-started)/1000,bps=elapsed?reader.used/elapsed:0;post('progress',{bytes:reader.used,total:file.size,frames:frame,bps,eta:bps?(file.size-reader.used)/bps:0,refills:reader.refills,collection:parser.collection});last=now}}await writer.close();await consumer;await parser.flush();post('complete',{tracks:parser.count,frames:frame,collection:parser.collection,bytes:reader.used,refills:reader.refills})}catch(error){try{await writer.abort(error)}catch{}throw error}}
self.onmessage=e=>{const m=e.data;if(m.type==='ack'&&waiting&&waiting.batchId===m.batchId){waiting.resolve();waiting=null}else if(m.type==='cancel'){canceled=true;if(waiting){waiting.reject(Error('Archive import was cancelled.'));waiting=null}}else if(m.type==='inspect'){canceled=false;inspect(m.file,{window:1024*1024}).catch(error=>post('error',{message:error.message||String(error)}))}else if(m.type==='start'){canceled=false;run(m).catch(error=>post('error',{message:error?.name==='OperationError'?'Archive authentication failed. Check the Web Passphrase or archive integrity.':error.message||String(error)}))}};
`;

export function startNovaAcWorker(
  file: File, 
  passphrase: string, 
  profile: string,
  onMessage: (msg: any) => void
) {
  const url = URL.createObjectURL(new Blob([workerSource], { type: 'text/javascript' }));
  const worker = new Worker(url);
  worker.onmessage = (e) => onMessage(e.data);
  worker.postMessage({ type: 'start', file, pass: passphrase, profileName: profile });
  return {
    worker,
    stop: () => {
      worker.terminate();
      URL.revokeObjectURL(url);
    }
  };
}
